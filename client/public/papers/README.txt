Put paper PDFs here (client/public/papers/).

Files in client/public/ are served at the site root, so a file named
mqp.pdf in this folder is reachable at /papers/mqp.pdf — which is what you'd
set as `pdfUrl` on a paper in src/data/content.ts.

You can delete this README once you've added your PDFs.
